# test-exam

## Запуск через Docker

```bash
docker compose up -d --build
```

Открыть: http://localhost:8080

## Запуск без Docker

Можно просто отдать папку через любой веб-сервер. Например:

```bash
python3 -m http.server 8080
```
